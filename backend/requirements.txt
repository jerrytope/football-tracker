# ════════════════════════════════════════════════════════════════════════
#  Django Backend — Python Requirements (pinned versions)
#  Install with: pip install -r requirements.txt
# ════════════════════════════════════════════════════════════════════════

# ── Django Core ──────────────────────────────────────────────────────────
Django==5.1.4
djangorestframework==3.15.2

# ── Authentication ───────────────────────────────────────────────────────
djangorestframework-simplejwt==5.3.1

# ── Asynchronous Task Queue ───────────────────────────────────────────────
celery==5.4.0
redis==5.2.1                      # Python Redis client (also used as result backend)
flower==2.0.1                     # Celery task monitoring dashboard

# ── Database ─────────────────────────────────────────────────────────────
psycopg2-binary==2.9.10           # PostgreSQL adapter

# ── File / Cloud Storage ─────────────────────────────────────────────────
django-storages==1.14.4           # S3 / GCS / Azure storage backends
boto3==1.35.93                    # AWS SDK (required by django-storages[s3])

# ── Configuration ────────────────────────────────────────────────────────
python-decouple==3.8              # Reads .env file into settings.py

# ── CORS ─────────────────────────────────────────────────────────────────
django-cors-headers==4.6.0        # Allow React/Vue frontend to call the API

# ── Error Tracking ───────────────────────────────────────────────────────
sentry-sdk==2.19.2                # Exception tracking (disable with SENTRY_DSN="")

# ── Computer Vision / CV Engine ────────────────────────────────────────
# These are required by the Celery worker when it imports cv_engine
opencv-python-headless==4.10.0.84
ultralytics==8.3.169
supervision==0.25.1
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
matplotlib==3.9.2
tqdm==4.66.5

# Note: install a GPU-compatible torch/torchvision build separately on RunPod
# for your CUDA version, since the exact wheel depends on the pod GPU runtime.

# ── Development / Testing ────────────────────────────────────────────────
pytest==8.3.4
pytest-django==4.9.0
factory-boy==3.3.1                # Model factories for tests
